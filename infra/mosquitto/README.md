# Mosquitto Infrastructure

MQTT broker configuration used by local development.

## What it does

- Runs broker on port `1883`
- Uses config in `mosquitto.conf`

## Useful commands (from repo root)

- `bun run nx run mosquitto:serve`
- `bun run nx run mosquitto:check`
